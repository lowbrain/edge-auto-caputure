# 第三者配布に向けた改善提案

対象コミット: `eac2b92`（コード）/ ブランチ `refactor/spa-event-driven`
作成日: 2026-08-11

配布形態は「`dist\edge-auto-capture\` をフォルダごと ZIP にして、Python 未導入の
Windows PC へ配る」（`README.md` の「配布方法」）を前提とする。

関連文書:

- [`ROADMAP.md`](ROADMAP.md) … **文書横断の優先順位。作業順はこれが正**
- [`IMPROVEMENTS.md`](IMPROVEMENTS.md) … 不具合・設計・運用・ページへの影響の指摘（A〜E 節）
- [`FEATURES.md`](FEATURES.md) … 機能提案（`F-` 接頭辞）
- [`HANDOFF.md`](HANDOFF.md) … 実装引き継ぎメモ

> 末尾の「配布前チェックリスト」は**配布作業の中での順序**。
> `D-C1`（無言終了）と `D-B1`（バージョン）は全体でも第 1 群に入るため、
> 配布を待たず先に直す価値がある（`ROADMAP.md` 参照）。

> **項番について**: 本ファイルの項目には **`D-` 接頭辞**を付ける
> （`IMPROVEMENTS.md` / `FEATURES.md` と衝突させないため）。

> **前提**: `USAGE.txt` は利用者向けの説明としては十分に親切で、
> SmartScreen 警告・`_internal` の注意・log.txt での切り分けまで既に押さえてある。
> 以下はそこに**残っている穴**の指摘である。

---

## A. 法務・コンプライアンス（最優先）

### D-A1. LICENSE ファイルがない

リポジトリにも配布フォルダにも `LICENSE` / `NOTICE` の類が**一切ない**（確認済み）。

ライセンス未指定のまま第三者に渡すと、受け取った側は法的には**何の権利も持たない**
（使用・複製・再配布のいずれも許諾されていない）状態になる。
社内配布でも「これ使っていいのか」が宙に浮く。

MIT / Apache-2.0 / 社内限定の独自条項、いずれにせよ**明示が必要**。
これは配布の前提条件。

### D-A2. サードパーティライセンス表記がない

`build.ps1` は `--collect-all playwright` で
**Playwright（Apache-2.0）とその Node ドライバを丸ごと同梱**している。
Apache-2.0 は再配布時にライセンス本文と著作権表示の同梱を求める。

配布フォルダに `THIRD-PARTY-NOTICES.txt` を置くこと。
`pip-licenses` 等で依存の一覧を吐き、`build.ps1` で `dist\` へコピーする 1 行を足せば自動化できる。

### D-A3. 保存物のプライバシー注意書きがない（実務上いちばん効く）

このツールは以下を保存する。

- **記録ON中に開いた全ページ**のフルスクリーンショットと全文テキスト
- `log.txt` に**訪問した URL を全部**
- 保存先は平文・アクセス制御なし

第三者が使えば、ログイン済みの業務システム、メール、人事情報、顧客の個人情報などが
**意図せず `output/` に溜まる**。しかも「記録中」バーは出るものの、
撮り忘れて放置すれば延々と溜まり続ける（`IMPROVEMENTS.md` B-3 のレート制限なしとも噛み合う）。

`USAGE.txt` に以下を明記した節を追加すること。

- 何が保存されるか（png / txt / _part.txt / log.txt の URL 履歴）
- 保存物の取り扱い責任は利用者にあること
- 不要になったら削除すること
- 撮影対象サイトの利用規約に触れる可能性があること

> 法的評価そのものは専門家の領域なので、ここでは
> **「注意書きを添える必要がある」という指摘に留める**。
> 条文の作成は法務に確認すること。

---

## B. サポート性（問い合わせに答えられるか）

### D-B1. バージョンがどこにも出ない（**最優先の実装項目**）

`version = "0.1.0"` は `pyproject.toml:3` にあるだけで、
**exe にもログにも UI にも一切出ていない**（`grep` で確認済み）。

結果、第三者から「動きません」と言われたときに
**「どの版をお使いですか」→「わかりません」** で詰む。配布物にとってこれは致命的。

| 対処 | 方法 |
|------|------|
| exe のプロパティに出す | PyInstaller の `--version-file` でバージョンリソースを埋める |
| ログの先頭に出す | `edge_auto_capture.py:378` の `log("=== edge-auto-capture 起動 ===")` にバージョンを入れる |
| 単一の出所にする | `__version__` を 1 箇所に置き、pyproject / build.ps1 / ログが参照する |

### D-B2. 環境情報がログに残らない

起動時に **Edge のバージョン・OS・実際に採用された設定値**を log.txt へ 1 行出すと、
切り分けが一気に楽になる。

`channel="msedge"` は環境の Edge に依存するため、Edge 側の更新で挙動が変わったときに
これが無いと原因に辿り着けない。

### D-B3. 問い合わせ先が書いていない

`USAGE.txt` に「うまくいかないとき」はあるが、
**それでも解決しない場合の連絡先**がない。
log.txt を添えて誰に送ればいいのかを 1 行足すこと。

---

## C. 第三者環境で壊れる箇所

### D-C1. 書き込み不可の場所に置かれると、何も起きずに死ぬ（要修正）

**実際に追える不具合。** `C:\Program Files\` など書き込み権限のない場所へ展開された場合:

1. `log()` は `except: pass`（`infra.py:66`）で**静かに失敗**
2. `set_log_dir` の `mkdir` も `except: pass`（`infra.py:50`）で**静かに失敗**
3. しかし `edge_auto_capture.py:321` の `config.output_dir.mkdir(...)` は
   **保護されておらず** `PermissionError` を送出
4. `--noconsole` ビルドなので stderr は誰にも見えない

→ **利用者から見ると「ダブルクリックしても何も起こらない」。ログもダイアログも残らない。**
`USAGE.txt` の「log.txt を確認してください」という案内も機能しない。

対処:

- `main()` の `mkdir` を `try` で包み、失敗時は `notify_fatal` でダイアログを出す
- 書き込み不可なら **`%LOCALAPPDATA%` へフォールバック**して動き続ける

### D-C2. ZIP から直接実行される

Windows は ZIP の中身をダブルクリックで実行できてしまい、
その場合 `_internal\` が解決されず失敗する。

`USAGE.txt` に `_internal` を消すなという注意はあるが、
**「必ず展開してから実行」「Program Files など権限のない場所には置かない」**は書かれていない。
D-C1 とセットで追記すること。

### D-C3. config.ini を失うと起動不能

削除・破損すると `notify_fatal` → `sys.exit(1)`（`config.py:55`）。
ダイアログは出るので D-C1 よりましだが、配布物としては
**既定値で起動して config.ini を書き直す**自己修復のほうが親切。サポート負荷が下がる。

### D-C4. 多重起動

`IMPROVEMENTS.md` A-5（プロファイル掃除の衝突）に加え、`output/` と `log.txt` の共有も起きる。
第三者は必ず二度押しする。単一インスタンス化（ミューテックス）か、少なくとも警告を出すこと。

---

## D. 導入障壁

### D-D1. 未署名 exe

`README.md` / `USAGE.txt` とも SmartScreen 警告に触れているが、
**企業環境では「警告」ではなく「ブロック」になる**ことがある
（AppLocker / WDAC / Intune のポリシー）。
「詳細情報 → 実行」で回避できない環境では、そもそも起動しない。

本格的に配る前提なら**コードサイニング証明書**が最も効く
（OV で警告軽減、EV なら SmartScreen の評価が即座に付く）。

> 年額のコストと発行手続きが要るため、これは技術判断ではなく**組織判断**。
> 配布規模が小さいなら、配布先の IT 部門に事前に許可リスト登録してもらう運用でも回せる。

### D-D2. アンチウイルスの誤検知

PyInstaller 製バイナリはヒューリスティックで誤検知されやすい傾向がある。
`--onedir` を使っている（`--onefile` より誤検知しにくい）のは既に良い判断。

それでも出る場合は、署名するかベンダーへ誤検知報告するのが現実的な対処。
**配布前に主要な AV で一度確認しておく**こと。

### D-D3. 完全性の検証手段がない

ZIP の **SHA256 を併記**すること。受け取り側が「正しいものを受け取ったか」を確認できる。
`build.ps1` の最後に `Get-FileHash` を出力する数行で済む。

---

## E. 配布物そのもの

- **サイズが未計測** — `--collect-all playwright` は Node ドライバごと同梱するため、
  100MB を超える可能性がある。配布経路（メール添付は不可、等）に影響するので実測すること。
- **`output/` を配布フォルダに含めるか** — `README.md` の構成図には
  `output\` が「実行後に生成される」とあるが、空フォルダを同梱しておくと
  D-C1 の権限問題に早く気づける（展開時点で書き込み可否が分かる）。

---

## 配布前チェックリスト（優先度順）

| # | 項目 | 該当項 | コスト | 分類 |
|---|------|--------|--------|------|
| 1 | LICENSE を決めて置く | D-A1 | 極小 | **必須** |
| 2 | バージョンを exe とログに出す | D-B1 | 小 | **必須** |
| 3 | `mkdir` 保護 + フォールバック | D-C1 | 小 | **必須** |
| 4 | USAGE.txt にプライバシー注意書き | D-A3 | 小 | **必須** |
| 5 | THIRD-PARTY-NOTICES.txt を同梱 | D-A2 | 小 | **必須** |
| 6 | USAGE.txt に展開場所・問い合わせ先 | D-C2 / D-B3 | 極小 | 推奨 |
| 7 | 環境情報を起動ログへ | D-B2 | 小 | 推奨 |
| 8 | ZIP の SHA256 併記 | D-D3 | 極小 | 推奨 |
| 9 | config.ini の自己修復 | D-C3 | 小 | 推奨 |
| 10 | 多重起動の抑止 | D-C4 | 小 | 推奨 |
| 11 | コードサイニング証明書 | D-D1 | 中（費用） | **組織判断** |

**1〜5 は配布前に必須**。特に以下の 2 つは質の違う問題である。

- **1（LICENSE）** … そもそも「配れない」
- **3（無言で死ぬ）** … 「配っても動かないのに原因が分からない」

1〜10 を合計しても半日程度の作業量。
11 だけは費用と手続きが絡むので、**着手前に判断を仰ぐこと**。
