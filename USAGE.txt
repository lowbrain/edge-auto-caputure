============================================================
 edge-auto-capture 使い方（配布版）
============================================================

■ これは何？
 Microsoft Edge で見ているページが切り替わる（URL やタブが変わる）たびに、
 その画面を自動で保存するツールです。保存されるのは次の2つです。
   ・フルページのスクリーンショット （.png）
   ・ページの全文テキスト           （.txt）
 むずかしい操作は不要です。いつもどおり Edge で見て回るだけで保存されます。

■ 動作条件
 - Windows
 - Microsoft Edge がインストール済みであること
   （Windows 11 なら標準で入っています）
 - Python は不要です（このフォルダだけで動きます）

■ 使い方（3ステップ）
 1. 同じフォルダの config.ini をメモ帳などで開き、必要なら設定します。
      start_url       … 起動時に最初に開くページ（例: https://www.google.com）
      output_dir      … 保存先フォルダ（既定: この exe と同じ場所の output）
      target_selector … ページの一部だけを抜き出したいときの CSS セレクタ（任意）
    ※ 変更したら上書き保存してください。設定を変えなくてもそのまま動きます。

 2. edge_auto_capture.exe をダブルクリックします。
    少し待つと Edge が開きます（黒いコンソール窓は出ません）。
    画面の上部に赤い帯で
      「● edge_auto_capture がこの画面をキャプチャ中です」
    と表示されます。これはキャプチャ中であることの目印で、正常な状態です。
    ※ この赤い帯は、保存される png / txt には写り込みません。消さずにそのままで大丈夫です。

 3. 開いた Edge で、いつもどおりページを見てください。
    ページを移動するたびに、output フォルダへ png と txt が自動で保存されます。

■ 保存先
 既定では exe と同じ場所の output フォルダに保存されます。
 別の場所へ保存したいときは、config.ini の output_dir に
 絶対パス（例: C:\Users\名前\Desktop\capture）を指定してください。

■ 止め方
 開いている Edge のウィンドウを閉じるだけです。
 このツールが起動した Edge の終了と、一時データの掃除まで自動で行います。
 （キー操作などは不要です）

■ うまく動かないときは
 - exe と同じ場所にできる log.txt に、動作の記録が残ります。
   「保存されない」「起動してもすぐ終わる」ときは、このファイルを開いて確認してください。
 - 設定の誤りや Edge が見つからない場合などは、内容を知らせる小さなダイアログが出ます。
   メッセージにしたがって config.ini を見直してください。

■ 初回起動時の注意
 署名のないプログラムのため、初回に Windows SmartScreen の警告が出ることがあります。
 その場合は「詳細情報」→「実行」で起動できます。
 また、_internal フォルダは動作に必要です。削除・移動しないでください。
============================================================
